# Сценарий видео — Advanced-курс, День 3 (Testing)

Видео без звука (та же схема, что для Дней 1-2) — смысл передаётся титрами
(ffmpeg `drawtext`) поверх записи экрана. Финал: `Day3ADV_final.mp4`, ~18:05
(1065.6с сырой записи + ~20с отдельного сегмента "Итог").

**Запись сделана и смонтирована (2026-07-22/23).** Титры ниже — уже по
факту реального прогона (сверено с логом сессии Claude Code,
`4c6dbc0c-45a8-4290-a263-cb2897c8ea0e.jsonl` в
`~/.claude/projects/C--Projects-CaptainBlood/`), не черновая прикидка.
**Важное расхождение с черновым планом**: в этом дубле Сценарий 5
(офтопик) реально провалился — три попытки дали три разных исхода, находка
задокументирована как настоящий пробел (нет keyword-gate для нейтрального
офтопика), не как success со сноской.

## Тайминг (по факту записи, секунды от начала)

| Диапазон | Содержание |
|---|---|
| 0:00-0:09 | Задача |
| 0:10-0:30 | Уровень 1 — статично (тесты уже написаны, диф из прошлой сессии) |
| 0:31-16:49 | Живой прогон скилла `day3-full-check` (код-тесты + smoke + отчёт) |
| 16:50-17:23 | Единый отчёт |
| 17:24-17:45 | Интеграция в flow |
| 17:45-18:05 | Итог (отдельный сгенерированный чёрный сегмент, текст по центру экрана, не внизу) |

### Раскладка внутри 0:31-16:49 (реальные моменты из лога сессии)

| t | Момент |
|---|---|
| 0:31 | Промпт "Прогони скилл day3-full-check" |
| 0:41-1:03 | Шаг 1 — повторный прогон, 13/13 PASSED |
| 1:06-1:35 | Поднимаю сервис локально (`./gradlew run`, `localhost:8080`) |
| 2:25-2:56 | Титр — список 5 сценариев |
| 2:58-3:13 | Сценарий 1 — health-check без авторизации |
| 3:14-3:30 | Сценарий 3 — неверный пароль → 401 |
| 3:32-3:50 | Сценарий 2, попытка 1 — вход, задаю вопрос про риф |
| 4:20-4:57 | Проблема 1: креды в URL → `location.href`, `fetch()` падает (методический артефакт) |
| 4:58-6:13 | Обход — повторный заход без креда в URL, Basic Auth из кэша Chrome |
| 6:21-7:16 | Ответ по теме (риф) получен, ~5с, источник `content_MorskieObitateli` |
| 7:17-7:39 | Сценарий 4 — интро, лимит 5/30с |
| 7:40-8:15 | Сценарий 4 — 6-й запрос отклонён |
| 8:46-9:32 | Сценарий 5, попытка 1 ("пицца") — капитан дал полный рецепт, БЕЗ увода темы |
| 9:44-10:42 | Читаю код — нет правила/gate на нейтральный офтопик |
| 11:48-12:43 | Сценарий 5, попытка 2 ("футбол") — сработало штатно, тема уведена |
| 14:08-15:10 | Сценарий 5, попытка 3 (снова "пицца") — языковой дрейф модели |
| 15:20-16:22 | Итог по Сценарию 5 — FAIL, недетерминированно, находка зафиксирована |
| 16:23 | Запись `Day3_FullCheck_Report.md` |

## 1. Задача (0:00-0:09)

Титр:

> Задание: два уровня тестирования — код-тесты (Уровень 1) и smoke-сценарии
> через MCP, агент сам ведёт браузер (Уровень 2). Минимум 3 файла тестов,
> 3-5 UI-сценариев, единый отчёт, интеграция в CI.

## 2. Уровень 1 — статичный блок (0:10-0:30)

Тесты написаны по-настоящему в прошлой сессии — не реконструировали
написание на камеру (тот же принцип, что в Дне 1). Показано статично: дифф
трёх файлов `src/test/kotlin/**`.

Титры:

> Тесты уже написаны в прошлой сессии. Промпт: «Найди непокрытые тестами
> модули, напиши unit-тесты минимум на 3, прогони, убедись что все проходят»

> Агент нашёл 3 модуля — RateLimiter, DocumentChunker, RecoveryAction —
> написал 13 тестов, все прошли с первого раза

## 3. Живой прогон — скилл day3-full-check (0:31-16:49)

Единственный живой промпт всего видео:

```
Прогони скилл day3-full-check
```

Скилл сам гоняет код-тесты (Шаг 1 — в этом вызове просто повторный прогон,
новых непокрытых модулей не было), поднимает сервис, проходит 5
сценариев, собирает отчёт. Точные титры и тайминг — см. таблицу выше;
дословные формулировки находок — по факту, не по черновику:

> Первая попытка зависла — «нет связи с кораблём». Playwright передал креды
> в URL, location.href их не убрал, fetch() на странице упал по
> спецификации. Прямой запрос отработал штатно — сервис не при чём.

> Сценарий 5, попытка 1: «Как приготовить пиццу?» → капитан дал ПОЛНЫЙ
> рецепт, без единого намёка на увод в морскую тему

> Смотрю код: правило есть только на пугающие/жестокие темы. Для
> нейтрального офтопика (еда, спорт) — ни одного правила, ни keyword-gate

> Попытка 2, другой офтопик-вопрос: «Расскажи про футбол» → капитан мягко
> увёл разговор в морскую тему — сработало как ожидалось

> Попытка 3, тот же вопрос про пиццу повторно → языковой дрейф модели
> (уезжает в другой алфавит) — честная заглушка вместо мусора

> Итог по Сценарию 5: три попытки — три разных исхода. Нет keyword-gate на
> нейтральный офтопик. FAIL — не чинили, зафиксировали находку в отчёте

## 4. Единый отчёт (16:50-17:23)

Титр:

> Отчёт объединяет оба уровня: 13/13 код-тестов + 4/5 smoke-сценариев
> (1 — реальная находка, не баг тестовой оснастки), скриншот на каждый шаг

## 5. Интеграция в flow (17:24-17:45)

Титр:

> Код-тесты — в CI на каждый PR. Smoke — по запросу через day3-full-check,
> включая триггер «задеплоил фичу — обнови и прогони заново» (скилл сам
> дописывает недостающие тесты)

## 6. Итог — отдельный сегмент (17:45-18:05, чёрный фон, текст по центру)

Титр:

> Полный цикл: агент сам написал тесты → прогнал → подтвердил (13/13).
> Агент сам провёл браузер по 5 сценариям → 4 пройдено, 1 вскрыл реальный
> пробел (нет keyword-gate для нейтрального офтопика) — не спрятал, а
> зафиксировал отдельно. Оба уровня и общий отчёт — под одним вызываемым по
> запросу скиллом.

## Заметки на память (монтаж)

- Источник: `C:\Users\sergio\Videos\Записи экрана\Day3ADV.mp4` (1065.6с,
  1898x1900, 30fps, без звука в выводе — `-an`).
- Тайминг титров сверен с логом сессии (`*.jsonl`), не с секундомером на
  глаз — отсюда точность до секунды в таблице выше.
- Стиль титров — общий (Arial 34pt, `line_spacing=2`, полупрозрачная
  плашка, низ по центру) кроме сегмента "Итог" — тот по центру экрана,
  отдельный чёрный `lavfi`-сегмент, не наложение на запись.
- Рабочие файлы монтажа (шрифт, txt титров, filter_complex) — в scratch
  текущей сессии, не в репозитории.
